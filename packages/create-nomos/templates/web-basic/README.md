# Nomos Web Basics

Adds a small, namespaced starter area under `src/nomos` plus `/nomos` routes that
show how to call the Nomos API from an Astro app.

This template is applied on top of an existing Astro project.
