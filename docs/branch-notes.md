<!-- Nomos-UI spec constraints: panels are runtime instances; PanelModules are TS modules imported in SSR+CSR; ResourceDefinitions are JSON-serializable data only; pages are assemblies that host panels; policy gating uses intents; accessibility is default. -->

# Branch Notes

- PanelModule migration underway.
